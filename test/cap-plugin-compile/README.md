# Test CAP Plugin: Compile

Tests the functionality around running `cds.compile` inside the browser.

It explicitly does **not** include sqlite and other dependencies to ensure that they stay optional.
